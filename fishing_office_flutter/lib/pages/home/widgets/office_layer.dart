import 'package:flutter/material.dart';

class OfficeLayer extends StatelessWidget {
  const OfficeLayer({super.key});

  @override
  Widget build(BuildContext context) {
    return const IgnorePointer(
      child: SizedBox.expand(),
    );
  }
}
